let firstNumber = 7;
let secondNumber = 9;

function add(one,other) {
    let suma = one + other;
    return suma;
}

console.log(add(firstNumber,secondNumber)); 

let wynik = add(firstNumber,secondNumber);

function subtraction(one,other) {
    let suma = one - other;
    return suma;
}
 
console.log(subtraction(firstNumber,secondNumber))

let resault = subtraction(firstNumber,secondNumber)

